[project]
name = "carbon-tracking-mcp"
version = "0.1.0"
description = "MCP server exposing session energy estimates from energy_estimate.py"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=4.0.3",
]
